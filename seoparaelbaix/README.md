# SEO Para El Baix

Sitio estático creado para servicios de SEO en El Baix. Contiene:

- `index.html` - Página principal
- `css/styles.css` - Estilos
- `js/main.js` - JavaScript para interactividad
- `blog/` - Artículos del blog (páginas estáticas)

Instrucciones rápidas:

1. Inicializar el repo local y hacer commit:

```powershell
cd "c:\Users\gerar\Desktop\programar web nueva\seoparaelbaix"
git init
git add .
git commit -m "Initial site: SEO Para El Baix"
```

2. Crear el repositorio en GitHub (opcional, usando GitHub CLI `gh`):

```powershell
gh repo create seoparaelbaix --public --source=. --remote=origin --push
```

Si no usas `gh`, crea el repositorio en https://github.com/new y luego:

```powershell
git remote add origin https://github.com/<TU_USUARIO>/<REPO>.git
git push -u origin main
```

3. Desplegar en Vercel (CLI o web):

Con CLI:

```powershell
vercel --prod
```

O conecta el repositorio desde https://vercel.com/import

Contacto y WhatsApp: modifica los números `34XXXXXXXXX` en `index.html` y `js/main.js`.

---
Generado automáticamente por asistente para preparar despliegue.
